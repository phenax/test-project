name: Helper

on: push

jobs:
  instructions:
    name: instructions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "BOOHOOOOO"
      - run: echo "Token:${{secrets.GITHUB_TOKEN}}"

