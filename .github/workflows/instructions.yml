name: Helper

on: [pull_request, issues]

jobs:
  instructions:
    name: instructions
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - name: Comment
        uses: actions/github-script@v3
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            console.log('ctx', JSON.stringify(context.issue, 0, 2))
            console.log('gh', JSON.stringify(github.pulls))
